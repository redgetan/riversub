<%= form_for @song do |f| %>

  <%= f.fields_for :media_sources, MediaSource.new do |m| %>
    <%= m.label :media_url %>
    <%= m.text_field :url %>
  <% end %>

  <%= f.submit %>
  <%= tag :input, :type => "button", :value => "Cancel", :id => "cancel"  %>

<% end %>
