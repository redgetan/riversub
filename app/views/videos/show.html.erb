<div id='player' data-repo='<%= @repo.serialize.to_json %>'>
  <div id='player-top' class='row'> 
    <div class='span12'> 
      <h5 id='repo_label'>
        <% if @repo.user %>
          <span id='repo_owner'><a href="<%= @repo.owner_profile_url %>"><%= @repo.owner %></a></span> /
        <% end %>
        <a href="<%= @repo.url %>"><%= @repo.video.name %></a>
      </h5>
      <h6 id='video_url'><%= @repo.video.url %></h6> 
    </div> 
    <div id='player-top-left' class='span6'> 
      <div id='media_container'> 
        <div id='media'></div> 
        <div id='subtitle_bar' class='span6 center'> 
          <span id='subtitle_display' class='span5 center'></span> 
        </div> 
      </div> 
    </div> 
    <div id='player-top-right' class='span6'> 
      <div id='subtitle_container'> 
        <div id='subtitle_list'></div> 
        <div id='controls_extra' class='row'> 
          <div class='btn-group pull-right'> 
          <!-- <div class='pull-right'>  -->
            <% if @repo.user == current_user %>
              <a id='subtitle_editor_btn' class='btn btn-info' href="<%= @repo.editor_url %>" target="_blank"><i class="icon-film"></i> Subtitle Editor</a>
            <% else %>
              Authored by: <a href="<%= @repo.owner_profile_url %>"><%= @repo.owner %></a> (View only)
            <% end %>
          <!-- </div>  -->
            <a id='download_btn' class='btn' href='<%= @repo.subtitle_download_url %>'><i class='icon-download-alt'></i> Download</a> 
          </div> 
        </div> 
      </div> 
    </div> 
  </div> 
</div>    