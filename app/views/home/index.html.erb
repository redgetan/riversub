<div id="songs">

  <p>
    Songs
  </p>
  <ul>
    <% @songs.each do |song| %>
      <li id="<%= song.id %>"><%= link_to song.name, "#", :class => "song" %></li>
    <% end %>
  </ul>

  </br>
  </br>

  <div id="new">
    <input type="button" name="" value="Add Song" id="add_song_btn"/>
  </div>
</div>
