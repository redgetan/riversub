<div class='row'>
  <%= render "user_profile" %>
</div>

<div class='row'>
  <div class="col-xs-8  col-xs-offset-2" >

    <% if user_signed_in? && current_user == @user %>
      <h4 class=''>Hello! </h4>
      <p>As a youtube producer, yasub allows you to create an official page where users can help increase your Youtube traffic by translating your videos and sharing it with their followers. </p> 
      <br>
      <h4 class=''>
        <a href="<%= new_page_url %>" class='new_producer_page_btn pull-right'>+ New Official Page</a>
      </h4>
    <% end %>

    <h4>Official Pages</h4>

    <ul class='producer_page_list_container'>
      <% @user.pages.each do |page| %>
        <li class='col-xs-2'>
          <a class='producer_page_list_item' href="<%= page.url %>">
            <div class='pull-left'>
              <div id='avatar' >
                <div class="thumbnail">
                  <%= image_tag page.thumbnail_url  %>
                </div>
              </div>
              <span class=""><%= page.title %></span>
              <% if user_signed_in? && current_user == @user %>
                <% unless page.status == "connected" %>
                  <a href="<%= page.status_url %>" class="page_status">remove permission error</a>
                <% end %>
              <% end %>
            </div>
          </a>
        </li>
      <% end %>
    </ul>


  </div>
</div>
