<div class='header-section col-xs-12'></div>

<div id='user_groups' class='col-xs-8 col-xs-offset-2'>
  <div class='row user_groups_container'>
    <div class='user_groups_container'>
      <% if (@groups = @user.groups).present? %>
        <div class='home_group_list_container col-md-12 col-xs-12'>
          <%= render partial: "groups/list", locals: { num_of_columns: 4 } %>
        </div>
      <% else %>
        <span>There's currently nothing here. </span>
        <% if user_signed_in? && current_user == @user %>
          <span>You can find topics to subscribe to <a href="<%= community_translations_url %>">here</a> or even create one yourself.</span>
        <% end %>
      <% end %>
    </div>
  </div>
</div>
