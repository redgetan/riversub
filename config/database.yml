defaults: &defaults
  adapter: mysql2
  username: root
  password: worldrix9
  host: localhost
  pool: 5
  timeout: 5000

development:
  database: lyrex_development
  <<: *defaults

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  database: lyrex_test
  <<: *defaults

production:
  database: lyrex_production
  <<: *defaults
